<table class='table'>
  <tr><%= image_tag @user.get_profile_image, size:'50×50' %></tr>
  <tr>
    <th>name</th>
    <th><%= @user.name %></th>
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= @user.introduction %></th>
  </tr>
  <tr><%= render 'relationships/follow', user: @user %></tr>
  <tr><%= render 'relationships/followed', user: @user %></tr>
</table>

<div class='row'>
  <% if @user.id == current_user.id %>
  <%= link_to edit_user_path(@user.id), class: "btn btn-outline-secondary btn-block edit_user_#{@user.id}" do %>
  <i class="fas fa-user-cog"></i>
  <% end %>
  <% end %>
</div>